# KeepGifPickerOpen

A simple Vencord plugin that prevents the GIF picker from closing after sending a GIF.

## Features
- Send multiple GIFs without reopening the GIF picker
- Lightweight
- No UI changes
